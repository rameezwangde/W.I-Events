import { Link } from 'react-router-dom'
import { ArrowUpRight } from 'lucide-react'
import { SEO, PageHero, SectionTitle, ContactCTA, Accordion } from '../components/Site'
import { services, faqs } from '../data/siteData'
export default function Services() { const schema = [{ '@context': 'https://schema.org', '@type': 'FAQPage', mainEntity: faqs.map(([q, a]) => ({ '@type': 'Question', name: q, acceptedAnswer: { '@type': 'Answer', text: a } })) }]; return <><SEO schema={schema} /><PageHero eyebrow="SERVICES" title="Nukkad Natak, CSR Campaign and Roadshow Services in India" copy="Integrated creative, performance and field execution for organisations that need to communicate with real audiences." /><section className="services-home section section--dark"><SectionTitle dark eyebrow="PERFORMANCE ARCHIVE" title="Ideas, Given a Voice" copy="One creative and production partner—from the first line of a script to the last location on a campaign route." /><div className="service-list">{services.map((s, i) => <Link to={`/services/${s.slug}`} className="service-row" key={s.slug}><span>{String(i + 1).padStart(2, '0')}</span><h3>{s.title}</h3><p>{s.short}</p><ArrowUpRight /></Link>)}</div></section><section className="section"><SectionTitle eyebrow="ONE TEAM, END TO END" title="From the First Insight to the Final Performance" /><div className="services-index">{services.map((s, i) => <Link key={s.slug} to={`/services/${s.slug}`}><span>0{i + 1}</span><div><h2>{s.title}</h2><p>{s.short}</p><small>{s.applications.join(' · ')}</small></div><ArrowUpRight /></Link>)}</div></section><section className="faq section section--parchment"><SectionTitle eyebrow="FREQUENTLY ASKED" title="Planning a Campaign? Start Here." /><Accordion items={faqs} /></section><ContactCTA /></> }
